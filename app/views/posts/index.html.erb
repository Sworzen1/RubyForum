<h1> Forum Posts </h1>
<% @posts.each do |post| %>
<div class="card">
<div class="card-body">
    <h3><%= post.title%></h3>
    <p><%= post.body%></p>
    <%= link_to "Read more", post_path(post), :class => 'btn btn-outline-secondary'%>
    </div>
    </div>
<%end%>